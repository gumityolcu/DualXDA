Bootstrap: docker
From: pytorch/pytorch:2.3.0-cuda11.8-cudnn8-devel

%files

  ../scikit-learn-dual /sklearn
  ../captum /captum

%post

  export "PATH=/opt/conda/bin:$PATH"
  pip install numpy \
    matplotlib \
    xmltodict \
    pyyaml \
    tqdm \
    scikit-image \
    scipy \
    seaborn \
    termcolor
  pip install zennit
  pip install tensorboard
  pip install /sklearn
  pip install torchmetrics
  pip install /captum
  pip install kronfluence

%runscript

  cd /mnt/src
  pwd
  ls
  if [ "$1" = "--trak" ]; then
    pip install traker[fast] --user
    shift
  fi
  python surrogate_evaluation.py "$@"
